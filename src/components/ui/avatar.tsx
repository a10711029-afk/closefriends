import { initials } from "@/lib/utils";
export function Avatar({ src, name, size="md", online=false }: { src?:string|null; name?:string|null; size?:"sm"|"md"|"lg"|"xl"; online?:boolean }) { const sizes={sm:"size-10 text-xs",md:"size-12 text-sm",lg:"size-16 text-lg",xl:"size-24 text-2xl"}; return <div className="relative shrink-0"><div className={`grid overflow-hidden place-items-center rounded-full bg-gradient-to-br from-[var(--brand)] to-[var(--brand-2)] font-bold text-white ${sizes[size]}`}>{src?<img src={src} alt={name||"Avatar"} className="size-full object-cover"/>:initials(name)}</div>{online&&<span className="absolute bottom-0 right-0 size-3.5 rounded-full border-[3px] border-[var(--bg)] bg-emerald-500"/>}</div>; }
