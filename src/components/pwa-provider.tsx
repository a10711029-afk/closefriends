"use client";
import { useEffect,useState } from "react"; import { PlusSquare,Share,WifiOff,X } from "lucide-react";
export function PwaProvider({children}:{children:React.ReactNode}){const[show,setShow]=useState(false),[online,setOnline]=useState(true);useEffect(()=>{if("serviceWorker" in navigator)navigator.serviceWorker.register("/sw.js");const standalone=matchMedia("(display-mode: standalone)").matches||(navigator as Navigator&{standalone?:boolean}).standalone;if(!standalone&&!localStorage.getItem("cc-install-dismissed")&&/iphone|ipad|ipod/i.test(navigator.userAgent))setTimeout(()=>setShow(true),1800);const sync=()=>setOnline(navigator.onLine);sync();addEventListener("online",sync);addEventListener("offline",sync);return()=>{removeEventListener("online",sync);removeEventListener("offline",sync)}},[]);const dismiss=()=>{localStorage.setItem("cc-install-dismissed","1");setShow(false)};return <>{!online&&<div className="fixed inset-x-0 top-0 z-[100] flex items-center justify-center gap-2 bg-[#161722] px-4 py-2 text-xs font-semibold text-white"><WifiOff size={14}/>Sem ligação à Internet</div>}{children}{show&&<div className="fixed inset-0 z-[90] flex items-end bg-black/40 p-3 pb-[max(12px,env(safe-area-inset-bottom))] backdrop-blur-sm"><div className="mx-auto w-full max-w-[410px] rounded-[30px] bg-[var(--surface)] p-5 shadow-2xl fade-up"><div className="flex items-start justify-between"><div><h2 className="text-lg font-bold">Instalar CloseChat</h2><p className="mt-1 text-sm muted">Usa-a como uma app no iPhone.</p></div><button onClick={dismiss} className="rounded-full bg-[var(--surface-2)] p-2"><X size={18}/></button></div><ol className="mt-5 space-y-3 text-sm"><li className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-full bg-[var(--surface-2)] font-bold">1</span>Abre a aplicação no Safari</li><li className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-full bg-indigo-500/15 text-[var(--brand)]"><Share size={16}/></span>Toca no botão Partilhar</li><li className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-full bg-violet-500/15 text-[var(--brand-2)]"><PlusSquare size={16}/></span>Escolhe “Adicionar ao ecrã principal”</li></ol><button onClick={dismiss} className="mt-5 w-full rounded-2xl bg-[var(--brand)] py-3.5 text-sm font-bold text-white">Percebi</button></div></div>}</>}
